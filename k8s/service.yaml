apiVersion: v1
kind: Service
metadata:
  name: bionic-platform
  namespace: bionic-platform
spec:
  selector:
    app: bionic-platform
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
